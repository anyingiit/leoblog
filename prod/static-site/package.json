{
  "name": "leoblog-production-static-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "playwright test",
    "serve": "node scripts/serve.mjs dist",
    "build": "astro build",
    "build:runner": "node scripts/build.mjs"
  },
  "dependencies": {
    "astro": "7.2.8"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "parse5": "7.3.0"
  }
}
